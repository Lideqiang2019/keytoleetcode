class Solution:
    def judgeCircle(self, moves: str) -> bool:
        '''
        字符串中左右字符相等，上下相等即可回来
        '''
        if moves.count('L')==moves.count('R') and moves.count('U')==moves.count('D'):
            return True
        return False
